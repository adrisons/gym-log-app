// Phase 0: no real adapter yet. This file exists only so the boundary rule
// has an infrastructure module to classify. Phase 2 replaces it with the
// FileSystemStorageAdapter / IndexedDbStorageAdapter.
export const INFRASTRUCTURE_LAYER = 'infrastructure';
